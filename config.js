// Backend URL configuration for hosted deployments.
// Set to your Render backend URL, e.g.:
// window.BACKEND_URL = 'https://your-backend.onrender.com';
// Leave empty ('') for same-origin deployment (Vercel serverless).
window.BACKEND_URL = '';

// When BACKEND_URL is empty, the app uses same-origin requests.
// The Vercel Python serverless function handles /api/* and /video_feed routes.
